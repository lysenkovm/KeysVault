[('table', 'actual', 'actual', 2, 'CREATE TABLE actual (\n        password_id INTEGER PRIMARY KEY,\n        resource TEXT,\n        login TEXT,\n        password TEXT,\n        description TEXT,\n        create_dt TEXT NOT NULL)'), ('table', 'archive', 'archive', 3, 'CREATE TABLE archive (\n        password_id INTEGER PRIMARY KEY,\n        resource TEXT,\n        login TEXT,\n        password TEXT,\n        description TEXT,\n        created_dt TEXT NOT NULL,\n        archived_dt TEXT NOT NULL)')]